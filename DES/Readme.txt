Assignment 6 Part B Ð DESAswin SrinivasanVinit PatelThe DES encryption and decryption is implemented as required in the specs. The extra credit portion of the assignment has also been implemented. It takes the in the key from a file and uses that for encryption and decryption. The key generation EC part is written, however we also have commented the hard-coded version just as a back up to the key generation portion. 